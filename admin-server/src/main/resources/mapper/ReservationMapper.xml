<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lab.booking.mapper.ReservationMapper">

    <resultMap id="ReservationResultMap" type="com.lab.booking.model.ReservationEntity">
        <id     property="reservationId" column="reservation_id"/>
        <result property="applicantId"   column="applicant_id"/>
        <result property="deviceId"      column="device_id"/>
        <result property="startTime"     column="start_time"/>
        <result property="endTime"       column="end_time"/>
        <result property="purpose"       column="purpose"/>
        <result property="status"        column="status"
                typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result property="reviewerId"    column="reviewer_id"/>
        <result property="reviewComment" column="review_comment"/>
        <result property="createdAt"     column="created_at"/>
    </resultMap>

    <select id="selectNextReservationId" resultType="java.lang.Long">
        SELECT COALESCE(MAX(reservation_id), 2000) + 1 FROM lab_reservation
    </select>

    <insert id="upsertReservation">
        INSERT INTO lab_reservation (
            reservation_id, applicant_id, device_id, start_time, end_time,
            purpose, status, reviewer_id, review_comment, created_at
        ) VALUES (
            #{reservationId}, #{applicantId}, #{deviceId}, #{startTime}, #{endTime},
            #{purpose},
            #{status, typeHandler=org.apache.ibatis.type.EnumTypeHandler},
            #{reviewerId}, #{reviewComment}, #{createdAt}
        )
        ON DUPLICATE KEY UPDATE
            applicant_id   = VALUES(applicant_id),
            device_id      = VALUES(device_id),
            start_time     = VALUES(start_time),
            end_time       = VALUES(end_time),
            purpose        = VALUES(purpose),
            status         = VALUES(status),
            reviewer_id    = VALUES(reviewer_id),
            review_comment = VALUES(review_comment),
            created_at     = VALUES(created_at)
    </insert>

    <select id="selectById" resultMap="ReservationResultMap">
        SELECT * FROM lab_reservation WHERE reservation_id = #{reservationId} LIMIT 1
    </select>

    <select id="selectAll" resultMap="ReservationResultMap">
        SELECT * FROM lab_reservation ORDER BY reservation_id
    </select>

</mapper>
